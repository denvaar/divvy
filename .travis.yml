language: python
python:
  - "3.5"
install:
  - pip install -r ./server/misc/requirements.txt
services:
  - docker
env:
  - DJANGO=1.9.7
  - DIVVY_ENV=development_docker
script:
  - python ./server/manage.py test apps

